import React from 'react';
import ReactDOM from 'react-dom/client';
import AppDrawer from './app-drawer';

const contatiner = document.querySelector('#root');
const root = ReactDOM.createRoot(contatiner);
root.render(<AppDrawer />);

/* exported capitalize */
// word is entered and it's first letter is capitalized and rest lower case
// string.length[0] = beginning applied uppercase() rest lowercase()
// new string returned string[] need to be added into empty string variable
// loop to transform each letter in the string
// loop with length of string

function capitalize(word) {
}
